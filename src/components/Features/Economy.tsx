import {useState} from 'react';import {motion} from 'framer-motion';import {BriefcaseBusiness,House,Store,Handshake,HardHat,Car,ArrowRight} from 'lucide-react';import {economy} from '../../data/content';import {Reveal} from '../ui';
const icons=[BriefcaseBusiness,House,Store,Handshake,HardHat,Car];
export default function Economy(){const [active,setActive]=useState(0);const selected=economy[active],Icon=icons[active];return <Reveal className="economy wrap"><div className="economy-intro"><p className="chapter">04 / МОЖЛИВОСТІ БЕЗ МЕЖ</p><h2>ЖИВА <em>ЕКОНОМІКА</em></h2><p className="muted">Маленькі рішення. Великі амбіції.<br/>Твій наступний крок змінює історію.</p><div className="economy-choices" aria-label="Сфери економіки">{economy.map((item,i)=>{const I=icons[i];return <button aria-pressed={active===i} className={active===i?'active':''} key={item.name} onClick={()=>setActive(i)}><I size={18}/>{item.name}<ArrowRight size={15}/></button>})}</div></div><div className="economy-visual"><div className="economy-orbit"><div className="orbit orbit-one"/><div className="orbit orbit-two"/><motion.div key={active} className="economy-core" initial={{opacity:0,scale:.85}} animate={{opacity:1,scale:1}}><Icon size={58} strokeWidth={1}/></motion.div><span className="orbit-label label-a">ІДЕЯ</span><span className="orbit-label label-b">МОЖЛИВІСТЬ</span><span className="orbit-label label-c">РЕЗУЛЬТАТ</span></div><div className="economy-description" aria-live="polite"><span className="gold-label">{selected.name}</span><h3>{selected.title}</h3><p>{selected.description}</p><div className="economy-steps">{selected.steps.map((s,i)=><span key={s}><small>0{i+1}</small>{s}</span>)}</div></div></div></Reveal>}
