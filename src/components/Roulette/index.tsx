import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, Coins, Gift, History, LoaderCircle, ShieldCheck, Sparkles } from 'lucide-react';
import { Brand } from '../ui';
import '../../styles/roulette.css';

type Quality = 'white'|'blue'|'purple'|'red'|'yellow';
type Prize = { id:number; quality:Quality; qualityLabel:string; title:string; rewardType:string; rewardValue:string; imageUrl:string|null; weight:number; isActive:boolean; sortOrder:number };
type HistoryItem = { id:number; quality:Quality; title:string; imageUrl:string|null; createdAt:string };
type WinItem = HistoryItem & { winId?:number; rewardType?:string; rewardValue?:string; sellPrice?:number; status?:'pending'|'claimed'|'sold' };
const qualityOrder:Quality[] = ['yellow','red','purple','blue','white'];
const qualityChance:Record<Quality,string> = { white:'55%',blue:'25%',purple:'12%',red:'6%',yellow:'2%' };

function session(){try{const value=JSON.parse(localStorage.getItem('prime-account-session')||'null');return value&&Number.isInteger(value.id)?value:null}catch{return null}}
function imageFor(prize:Prize|HistoryItem){return prize.imageUrl||'/assets/official-brand-original.webp'}

export default function Roulette(){
  const [player] = useState(session);
  const [prizes,setPrizes] = useState<Prize[]>([]);
  const [history,setHistory] = useState<HistoryItem[]>([]);
  const [wins,setWins] = useState<WinItem[]>([]);
  const [freeSpins,setFreeSpins] = useState(0);
  const [balance,setBalance] = useState(0);
  const [spinPrice,setSpinPrice] = useState(89);
  const [selected,setSelected] = useState<Prize|null>(null);
  const [rolling,setRolling] = useState(false);
  const [error,setError] = useState('');
  const [loaded,setLoaded] = useState(false);

  async function load(){if(!player)return;try{const response=await fetch(`/api/roulette?playerId=${player.id}`);const data=await response.json();if(!response.ok)throw new Error(data.error||'Рулетка недоступна');setPrizes(data.prizes||[]);setHistory(data.history||[]);setWins((data.wins||[]).map((item:WinItem)=>({...item,winId:item.id})));setFreeSpins(Number(data.freeSpins)||0);setBalance(Number(data.donate??data.balance)||0);setSpinPrice(Number(data.spinPrice)||89);setError('')}catch(value){setError(value instanceof Error?value.message:'Рулетка недоступна')}finally{setLoaded(true)}}
  useEffect(()=>{load()},[player?.id]);
  const grouped=useMemo(()=>qualityOrder.map(quality=>({quality,items:prizes.filter(prize=>prize.quality===quality)})).filter(group=>group.items.length),[prizes]);
  function rouletteSound(){try{const AudioContextClass=window.AudioContext||((window as unknown as {webkitAudioContext?:typeof AudioContext}).webkitAudioContext);if(!AudioContextClass)return;const context=new AudioContextClass();const oscillator=context.createOscillator();const gain=context.createGain();oscillator.type='triangle';oscillator.frequency.setValueAtTime(170,context.currentTime);oscillator.frequency.exponentialRampToValueAtTime(65,context.currentTime+1.8);gain.gain.setValueAtTime(.045,context.currentTime);gain.gain.exponentialRampToValueAtTime(.001,context.currentTime+1.8);oscillator.connect(gain).connect(context.destination);oscillator.start();oscillator.stop(context.currentTime+1.8)}catch{/* sound is optional and may be blocked by the browser */}}
  async function spin(){if(!player||rolling||(!freeSpins&&!balance))return;setRolling(true);setError('');rouletteSound();try{const response=await fetch('/api/roulette',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'spin',playerId:player.id})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Не вдалося обертати рулетку');setFreeSpins(Number(data.freeSpins)||0);setBalance(Number(data.balance)||0);setTimeout(()=>{setSelected(data.prize);setWins(current=>[{...data.prize,winId:data.winId,id:data.winId,sellPrice:data.sellPrice,status:'pending',createdAt:new Date().toISOString()},...current].slice(0,30));setHistory(current=>[{...data.prize,createdAt:new Date().toISOString()},...current].slice(0,30));setRolling(false)},1900)}catch(value){setError(value instanceof Error?value.message:'Не вдалося обертати рулетку');setRolling(false)}}
  async function settle(win:WinItem, action:'claim'|'sell'){if(!player||!win.winId)return;try{const response=await fetch('/api/roulette',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,playerId:player.id,winId:win.winId})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Не вдалося обробити виграш');setWins(current=>current.map(item=>item.winId===win.winId?{...item,status:action==='sell'?'sold':'claimed'}:item));if(action==='sell')setBalance(current=>current+Number(data.amount||0));else if(data.directGameCredit) load()}catch(value){setError(value instanceof Error?value.message:'Не вдалося обробити виграш')}}
  if(!player)return <div className="roulette-guest"><Brand/><Gift size={42}/><h1>РУЛЕТКА ДОСТУПНА В КАБІНЕТІ</h1><p>Увійдіть до особистого кабінету, щоб отримувати призи та бачити історію виграшів.</p><a className="button gold" href="/account">УВІЙТИ ДО КАБІНЕТУ</a></div>;
  return <div className="roulette-page"><header className="roulette-header"><a href="/account" className="roulette-back"><ArrowLeft size={16}/> КАБІНЕТ</a><Brand/><span className="roulette-player">{player.nickname}</span></header><main className="roulette-main"><div className="roulette-heading"><div><p className="eyebrow"><span/> PRIME REWARDS</p><h1>РУЛЕТКА</h1><p>Випробуйте удачу та отримуйте нагороди зі світу PRIME RP.</p></div><div className="roulette-wallet"><span>ВАШ БАЛАНС</span><strong><Coins size={20}/> {balance}</strong></div></div>{error&&<p className="roulette-error">{error}</p>}<section className="roulette-history roulette-history-top"><div className="roulette-section-title"><h2><History size={20}/> ОСТАННІ ВИГРАШІ</h2><span>Заберіть приз або продайте його</span></div><div className="roulette-history-row">{wins.length?wins.slice(0,8).map(item=><article className={`history-card quality-${item.quality}`} key={item.winId||item.id}><img src={imageFor(item)} alt=""/><strong>{item.title}</strong>{item.status==='pending'?<div className="history-actions"><button onClick={()=>settle(item,'claim')}>ЗАБРАТИ</button><button onClick={()=>settle(item,'sell')}>ПРОДАТИ · {item.sellPrice}</button></div>:<small>{item.status==='sold'?'ПРОДАНО':'ЗАБРАНО'}</small>}</article>):<p>Ви ще не вигравали призів.</p>}</div></section><section className="roulette-machine"><div className="roulette-pointer"/><div className={`roulette-track ${rolling?'rolling':''}`}>{(selected?[selected,...prizes]:prizes).slice(0,10).map((prize,index)=><article className={`roulette-result-card quality-${prize.quality}`} key={`${prize.id}-${index}`}><img src={imageFor(prize)} alt=""/><strong>{prize.title}</strong></article>)}</div><div className="roulette-center"><Sparkles size={27}/><span>{rolling?'ОБЕРТАЄМО…':selected?selected.title:'ОБЕРІТЬ НАГОРОДУ'}</span></div></section><div className="roulette-actions"><div className="roulette-free"><Gift size={18}/><span>Безкоштовних обертань<strong>{freeSpins}</strong></span></div><button className="button gold roulette-spin" disabled={rolling||(!freeSpins&&!balance)} onClick={spin}>{rolling?<LoaderCircle className="spin-icon" size={18}/>:<Sparkles size={18}/>} {freeSpins?'КРУТИТИ БЕЗКОШТОВНО':'КРУТИТИ РУЛЕТКУ'}</button><span className="roulette-hint"><ShieldCheck size={15}/> Реалістична прокрутка зі звуком · 10 призів у стрічці</span></div><section className="roulette-prizes"><div className="roulette-section-title"><h2>ЙМОВІРНИЙ ДРОП</h2><span>Білий має найбільший шанс, жовтий — найменший</span></div>{loaded?grouped.map(group=><div className={`prize-quality quality-${group.quality}`} key={group.quality}><div className="quality-heading"><h3>{group.items[0].qualityLabel}</h3><b>{qualityChance[group.quality]}</b></div><div className="prize-grid">{group.items.map(prize=><article className="prize-card" key={prize.id}><img src={imageFor(prize)} alt=""/><strong>{prize.title}</strong><small>{prize.rewardType}</small></article>)}</div></div>):<p>Завантаження призів…</p>}</section></main></div>;
}
