import {useContext} from 'react';import {ArrowUpRight} from 'lucide-react';import {Reveal,SectionTitle,OverlayContext} from '../ui';import type {NewsItem} from '../../types';
export default function News({items,error}:{items:NewsItem[];error:string}){const show=useContext(OverlayContext);return <Reveal id="news" className="news wrap"><SectionTitle eyebrow="БУДЬ У ЦЕНТРІ ПОДІЙ" title="ОСТАННІ НОВИНИ" accent="PRIME"><button className="text-link" onClick={()=>show({title:'Усі новини PRIME',body:items.length?items.map(n=>`${n.title}\n${n.body}`).join('\n\n'):'Офіційних публікацій поки немає.'})}>ВСІ НОВИНИ<ArrowUpRight size={18}/></button></SectionTitle>{error?<p role="status">{error}</p>:<div className="news-grid">{items.map((n,i)=><button className={`news-card ${i===0?'featured':''}`} key={n.id} onClick={()=>show({title:n.title,body:n.body,image:n.image})}><div className="news-image"><img src={n.image} alt=""/></div><div className="news-content"><div className="news-meta"><span>{n.category}</span><span>{n.isPlaceholder?'ПРИКЛАД ПУБЛІКАЦІЇ':'НОВИНИ PRIME'}</span></div><h3>{n.title}</h3><span className="news-bottom">ДІЗНАТИСЯ БІЛЬШЕ<ArrowUpRight size={20}/></span></div></button>)}</div>}<p className="content-note">Новини завантажуються з бази даних і редагуються в адмін-панелі.</p></Reveal>}
