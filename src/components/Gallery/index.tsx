import {useContext} from 'react';import {Expand} from 'lucide-react';import {Art,OverlayContext,Reveal,SectionTitle} from '../ui';import ProjectGallery from '../ProjectGallery';
export const gallery=[{image:'hero',title:'МІСТО ПІСЛЯ ЗАХОДУ СОНЦЯ',label:'01 / АТМОСФЕРА'},{image:'vehicle',title:'ХАРАКТЕР У КОЖНІЙ ДЕТАЛІ',label:'02 / АВТОМОБІЛІ'},{image:'city',title:'ЗНАЙОМА УКРАЇНА',label:'03 / ВІДКРИТИЙ СВІТ'}];
export default function Gallery(){const show=useContext(OverlayContext);return <><Reveal className="gallery wrap"><SectionTitle eyebrow="ВІДЧУЙ АТМОСФЕРУ" title="СВІТ" accent="PRIME RP"><p className="muted small">Оригінальні концепт-арти<br/>Не ігрові скриншоти</p></SectionTitle><div className="gallery-layout">{gallery.map((g,i)=><button key={g.image} className={`gallery-item gallery-${i}`} onClick={()=>show({title:g.title,galleryIndex:i})} aria-label={`Відкрити зображення: ${g.title}`}><Art name={g.image} alt={g.title}/><div className="gallery-overlay"><span>{g.label}</span><h3>{g.title}</h3></div><span className="expand-icon"><Expand size={20}/></span></button>)}</div></Reveal><ProjectGallery/></>}
