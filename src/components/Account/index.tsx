import {FormEvent,useState} from 'react';
import {ArrowLeft,Coins,LogIn,LogOut,Mail,ShieldCheck,UserRound,Wallet} from 'lucide-react';
import {Brand} from '../ui';
type Player={id:number;nickname:string;login:string;email:string|null;level:number;exp:number;online:number;money:number;donate:number};
const fmt=(v:number)=>new Intl.NumberFormat('uk-UA').format(Number(v)||0);
export default function Account(){const [mode,setMode]=useState<'login'|'reset'>('login'),[login,setLogin]=useState(''),[password,setPassword]=useState(''),[email,setEmail]=useState(''),[player,setPlayer]=useState<Player|null>(null),[error,setError]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setMessage('');setBusy(true);try{if(mode==='reset'){setMessage('Якщо таку електронну адресу зареєстровано, інструкції буде надіслано на неї.');return}const r=await fetch('/api/auth-login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({login,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Не вдалося увійти');setPlayer(d.player)}catch(e){setError(e instanceof Error?e.message:'Помилка авторизації')}finally{setBusy(false)}}if(player)return <div className="account-page"><header className="account-header"><Brand/><button className="account-back" onClick={()=>setPlayer(null)}><LogOut size={16}/> ВИЙТИ</button></header><main className="account-main"><div className="account-dashboard-heading"><div><p className="eyebrow"><span/> PERSONAL SPACE</p><h1>КАБІНЕТ <em>{player.nickname}</em></h1><p className="account-muted">Ігровий профіль PRIME RP та особиста статистика.</p></div><div className="account-level"><span>РІВЕНЬ</span><strong>{player.level}</strong></div></div><section className="account-grid"><article className="account-card account-card-wide"><div className="account-card-icon"><UserRound/></div><div><span className="account-label">ІГРОВИЙ ПРОФІЛЬ</span><h2>{player.nickname}</h2><p>Логін: {player.login}<br/>Електронна пошта: {player.email||'Не вказано'}</p></div><span className="account-status"><i/> {player.online?'У ГРІ':'НЕ В МЕРЕЖІ'}</span></article><article className="account-card"><Wallet/><span className="account-label">ІГРОВІ ГРОШІ</span><strong>{fmt(player.money)} ₴</strong></article><article className="account-card"><Coins/><span className="account-label">ДОНАТ-БАЛАНС</span><strong>{fmt(player.donate)}</strong></article><article className="account-card"><ShieldCheck/><span className="account-label">ДОСВІД</span><strong>{fmt(player.exp)}</strong><small>до наступного рівня</small></article></section><a href="/" className="account-home"><ArrowLeft size={16}/> НА ГОЛОВНУ</a></main></div>;return <div className="account-page"><header className="account-header"><Brand/><a className="account-back" href="/"><ArrowLeft size={16}/> НА ГОЛОВНУ</a></header><main className="account-auth"><div className="account-auth-copy"><p className="eyebrow"><span/> PRIME RP COMMUNITY</p><h1>ТВОЯ ІСТОРІЯ<br/><em>ПОЧИНАЄТЬСЯ ТУТ</em></h1><p>Увійди до особистого кабінету, щоб переглянути профіль, прогрес та ігрові ресурси.</p></div><form className="account-form" onSubmit={submit}><div className="account-form-heading"><span>{mode==='login'?'01':'02'}</span><h2>{mode==='login'?'ВХІД ДО КАБІНЕТУ':'ВІДНОВЛЕННЯ'}</h2></div>{mode==='login'?<><label>ЛОГІН<input value={login} onChange={e=>setLogin(e.target.value)} required/></label><label>ПАРОЛЬ<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label></>:<label>ЕЛЕКТРОННА ПОШТА<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label>}{error&&<p className="account-error">{error}</p>}{message&&<p className="account-message">{message}</p>}<button className="button gold account-submit" disabled={busy}>{busy?'ПЕРЕВІРЯЄМО…':mode==='login'?<><LogIn size={17}/> УВІЙТИ</>:<><Mail size={17}/> НАДІСЛАТИ ПОСИЛАННЯ</>}</button><button type="button" className="account-switch" onClick={()=>{setMode(mode==='login'?'reset':'login');setError('');setMessage('')}}>{mode==='login'?'Забули пароль? Відновити через email':'Повернутися до входу'}</button></form></main><footer className="account-footer"><span>© 2026 PRIME RP</span><span>ТВІЙ СВІТ. ТВОЇ ПРАВИЛА.</span></footer></div>}
