import { useEffect, useState } from 'react';
import { Menu, X, UserRound, ArrowUpRight } from 'lucide-react';
import { AnimatePresence,motion } from 'framer-motion';
import { Brand, ConfigLink } from '../ui';
const nav=[['ГОЛОВНА','home'],['ПРО ПРОЄКТ','about'],['ЯК ПОЧАТИ','start'],['НОВИНИ','news'],['ВІКІ','wiki'],['ДОНАТ','donate-shop'],['СЕРВЕРИ','servers']];
export default function Header(){const [scrolled,setScrolled]=useState(false),[open,setOpen]=useState(false);useEffect(()=>{const fn=()=>setScrolled(window.scrollY>40);fn();window.addEventListener('scroll',fn,{passive:true});return()=>window.removeEventListener('scroll',fn)},[]);useEffect(()=>{if(!open)return;const fn=(e:KeyboardEvent)=>{if(e.key==='Escape'){setOpen(false);document.getElementById('menu-toggle')?.focus()}};document.addEventListener('keydown',fn);return()=>document.removeEventListener('keydown',fn)},[open]);return <header className={`header ${scrolled||open?'scrolled':''}`}><div className="header-inner"><Brand/><nav className="desktop-nav" aria-label="Основна навігація">{nav.map(([n,id])=><a key={id} href={`#${id}`}>{n}</a>)}</nav><div className="header-actions"><ConfigLink configKey="forumUrl" className="header-link">ФОРУМ<ArrowUpRight size={12}/></ConfigLink><ConfigLink configKey="shopUrl" className="header-link">МАГАЗИН</ConfigLink><ConfigLink configKey="accountUrl" className="button account" label="Особистий кабінет"><UserRound size={17}/><span>ОСОБИСТИЙ КАБІНЕТ</span></ConfigLink></div><button id="menu-toggle" className="menu-toggle icon-button" aria-label={open?'Закрити меню':'Відкрити меню'} aria-expanded={open} aria-controls="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div><AnimatePresence>{open&&<motion.nav id="mobile-menu" className="mobile-menu" aria-label="Мобільна навігація" initial={{opacity:0,height:0}} animate={{opacity:1,height:'auto'}} exit={{opacity:0,height:0}} onClick={()=>setOpen(false)}>{nav.map(([n,id])=><a key={id} href={`#${id}`}>{n}<ArrowUpRight size={18}/></a>)}<ConfigLink configKey="forumUrl">ФОРУМ</ConfigLink><ConfigLink configKey="shopUrl">МАГАЗИН</ConfigLink><ConfigLink configKey="accountUrl" className="button gold">ОСОБИСТИЙ КАБІНЕТ</ConfigLink></motion.nav>}</AnimatePresence></header>}
